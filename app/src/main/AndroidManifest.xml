<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pm.pmproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.WorkoutListActivity" />
        <activity android:name=".view.activity.ProgressListActivity" />
        <activity android:name=".view.activity.WorkoutDetalisActivity" />
        <activity android:name=".view.activity.ProgressDetailsActivity" />
        <activity android:name=".view.activity.NewWorkoutActivity"></activity>
    </application>

</manifest>